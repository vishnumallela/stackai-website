# xAI API key — used server-side to mint short-lived realtime voice tokens
# for the "Talk to us" voice assistant. The key never reaches the browser.
# Local dev: copy this file to `.env` and set the key, then `bun run dev`.
# Production (Vercel): add XAI_API_KEY in the project's Environment Variables.
XAI_API_KEY=
